# zenoh mTLS material (distributed separately as a zip; never commit)
tls/
tls*.zip
*tls*.zip
*.pem
*.key

.env
output/
*.log
*.pyc
__pycache__/
.coverage

# AI related local notes (do not commit)
.claude
/CLAUDE.md
/MEMORY.md

# コアダンプ。RViz コンテナは working_dir が /scripts (= ./scripts のバインドマウント) な
# ので、rviz2 が落ちるとホストの scripts/ に数百MBのコアが降ってくる。
core
core.*
