# 遠隔操作 joy ルーティング仕様

遠隔操作PCの `racing_kart_manager` が、手元のジョイスティック入力を対象車両へどう配るかを定める。

- 実装: `manager/racing_kart_manager_core.py`（純ロジック）、`manager/racing_kart_manager.py`（ROS ノードと GUI）
- テスト: `manager/tests/`（§10 の観点に対応する）
- レース開始・終了の MQTT 通知: [`race-notification.md`](race-notification.md)

## 1. 役割

manager は **joy の中継器**である。オペレータが選んだ宛先へ、joy をマスクして配る。

判断に使うのは次の3つだけ。

- オペレータが GUI で選んだ選択
- オペレータが GUI で押した一斉指令（§4.3）
- 受信した joy の中身

車両から届くテレメトリは購読しない（§9）。

- **REQ-01** manager は1つのプロセスである。joy の中継と選択の GUI を同じプロセスが持つ。
- **REQ-02** GUI を開けない環境では起動しない。画面のない運用は想定しない。
- **REQ-03** GUI の描画と操作は joy の中継を妨げてはならない。joy の中継は ROS の実行スレッドで行い、GUI はメインスレッドで動かす。
- **REQ-04** ROS の executor は単一スレッドとする。joy のコールバックが到着順に直列で走ることに依存している（レース通知の立ち上がり判定）。
- **REQ-05** 終了は shutdown → ROS スレッドの join → ノードの破棄 の順に行う。executor が保持しているノードを別スレッドから壊さない。

## 2. 対象車両

- **REQ-06** 対象車両は起動引数で与える。既定値を持たない。
- **REQ-07** 空・未知の車両ID・重複を含む引数は解釈失敗とし、manager は起動しない。
- **REQ-08** 「全台」は対象車両の全部を指す。運用に存在する車両の全部ではない。

## 3. 選択

- **REQ-09** 選択は次の3状態のいずれか。**未選択** / **単車**（車両IDひとつ） / **全台**。
- **REQ-10** 起動直後は未選択。
- **REQ-11** 選択が変わるのは GUI の操作によるときだけ。joy は選択を変えない。
- **REQ-12** 選択はいつでも即座に切り替わる。前提条件を課さない。joy の中身も、直前の選択も問わない。
- **REQ-28** GUI の「レース開始」ボタン（§4.3）は選択を**全台**にする。GUI の操作なので REQ-11 と矛盾しない。
  レース中は全台選択が運用の既定であり、そのあとの手元の joy（Y / X / 緊急停止の解除）がそのまま全車へ効く。

## 4. joy の送出

### 4.1 契機

- **REQ-13** joy を1つ受信するごとに、対象車両**全部**へ1つずつ publish する。タイマーでは送らない。
  joy が途絶えれば車両にも新鮮な joy が届かなくなり、車両側の5秒ウォッチドッグが働く。
  タイマー送出を入れると、この生存チェーンが切れる。
- **REQ-14** 未選択であっても送出する。宛先を止めない。
  送出を止めた車両は5秒後に緊急停止がラッチし、選択し直しても解除操作なしには動かせなくなる。

### 4.2 マスク

- **REQ-15** 送出する joy は axes 8 要素・buttons 11 要素とする。
- **REQ-16** 宛先が**選択車**（単車選択ならその1台、全台選択なら全部）なら、受信した joy をそのまま送る。
- **REQ-17** 宛先が**非選択車**なら、axes を無操作値（§5.1）に、buttons を 0 に置き換えて送る。緊急停止ボタン（REQ-20）だけは例外。
- **REQ-18** 受信した joy の axes / buttons が REQ-15 の要素数と異なるときは不正入力とみなし、**全宛先を非選択車として扱う**。緊急停止ボタンは読み取れた範囲で判定する。
  壊れた入力で操縦させない。ただし壊れていても緊急停止だけは通す。
- **REQ-19** `header.stamp` は受信した joy の値を引き継ぐ。

| 宛先 | axes | LB / RB / START / BACK | LSB / RSB | A / B / X / Y |
| --- | --- | --- | --- | --- |
| 選択車 | 素通し | 素通し | 素通し | 素通し |
| 非選択車 | 無操作値 | 素通し（§6） | 0 | 0 |

### 4.3 一斉指令

GUI の「レース開始」「レース終了」ボタン。選択に関係なく**対象車両の全部**へ、
`control_mode` を変えるボタンを重ねて送る。レースの号令を1クリックで全車へ渡すためのもので、
1台ずつ選び直して Y を押していく手順を置き換える。

- **REQ-29** 一斉指令を起こすのは GUI のこの2つのボタンだけである。joy は一斉指令を起こさない。
- **REQ-30** 一斉指令は対象車両の全部へ届く。選択を問わない。未選択でも届く。
- **REQ-31** 重ねるものは次のとおり。ここに書いていない軸とボタンには触れない。

  | ボタン | 重ねるもの | 車両側の効果 |
  | --- | --- | --- |
  | レース開始 | ボタン Y = `1` | `control_mode` を `AUTONOMOUS` へ |
  | レース終了 | ボタン X = `1`、アクセル軸 = `+1.0` | `control_mode` を `AUTONOMOUS_STEER_ONLY` へ、スロットルカット |

  レース終了でアクセルを落とすのは、`AUTONOMOUS_STEER_ONLY` ではアクセルが joy 側に移るため。
  落とさないと、全台選択でトリガーを踏んだまま終了ボタンを押したときに、自動操舵のまま加速する。
  ブレーキ軸には触れないので、終了と同時に手元のブレーキを踏める。

- **REQ-32** 一斉指令は**受信した joy に重ねて**送る。押した瞬間には送らず、次に joy を受け取ったときに乗る。
  ボタンを契機に送出すると、joy が途絶えても車両に新鮮な joy が届き、REQ-13 の生存チェーンが切れる。
  押下から joy へ乗るまでの遅れは最大1フレーム（20Hz で 50ms）。
- **REQ-33** 1回の押下につき、連続する `COMMAND_REPEAT` 個（10、20Hz でおよそ0.5秒）の joy に重ねる。
  車両側の `control_mode` は1フレーム届けばラッチするが、joy の QoS は depth 1 で、
  無線の取りこぼしでその1フレームが落ちるとボタンが効かなかったように見える。
- **REQ-34** 繰り返しの途中で別の指令を押したら、**あとの指令で置き換える**。前の指令の残りは捨てる。
- **REQ-35** 一斉指令は緊急停止のマスク（REQ-20）を打ち消さない。同時に成立すれば両方立つ。
- **REQ-36** 一斉指令は、要素数が規定と異なる joy を受け取ったフレームでも届く（REQ-18 の対象外）。
  指令の出どころは GUI であり、joy がどう壊れているかとは関係しない。
  送り出す joy 自体は REQ-15 の要素数に直っているので、車両側も読める。

一斉指令はレースの開始・終了の MQTT 通知も出す。条件と時刻の扱いは
[`race-notification.md`](race-notification.md) §4 を見ること。

#### 知っておく必要がある挙動

**レース終了を押しても止まらない。** `AUTONOMOUS_STEER_ONLY` は自動操舵のまま惰行する状態であり、
ブレーキは掛からない。止めるのは緊急停止（§6）の役目で、一斉指令はそれを置き換えない。

**緊急停止中でも指令は届く。** 緊急停止を押しながらレース開始を押すと、車両は緊急停止をラッチしたまま
`control_mode` だけ `AUTONOMOUS` になる。解除した瞬間に自動走行が始まる。

**届いたかどうかは分からない。** テレメトリを購読しないため、`control_mode` が実際に切り替わったかを
遠隔側から確認する手段は無い（§9）。GUI のボタンが光るのは押したことの表示であって、
届いたことの表示ではない。

## 5. キーバインド

`racing_kart_interface` の `src/racing_kart_driver/src/keybind/joystick.hpp` の複製である。
別リポジトリのため import できない。変更するときは両方を直すこと。

### 5.1 axes（8要素）

アクセルとブレーキは **0.0 ではなく +1.0 が無操作**である。車両側は `clamp((1.0 - axes[i]) / 2.0, 0, 1)`
で踏み込み量を作るため、0 で埋めるとアクセル 50%・ブレーキ 50% を踏んだ扱いになる。

| index | 名前 | 無操作値 |
| --- | --- | --- |
| 0 | Steer | 0.0 |
| 1 | LeftStickVertical（未使用） | 0.0 |
| 2 | Brake | +1.0 |
| 3 | RightStickHorizontal（未使用） | 0.0 |
| 4 | RightStickVertical（未使用） | 0.0 |
| 5 | Accel | +1.0 |
| 6 | DpadHorizontal（ギア） | 0.0 |
| 7 | DpadVertical（ギア） | 0.0 |

### 5.2 buttons（11要素）

| index | 名前 | 車両側での意味 |
| --- | --- | --- |
| 0 | A | control_mode を MANUAL へ |
| 1 | B | 未使用 |
| 2 | X | control_mode を AUTONOMOUS_STEER_ONLY へ |
| 3 | Y | control_mode を AUTONOMOUS へ |
| 4 | LB | 緊急停止 |
| 5 | RB | 緊急停止 |
| 6 | START | 緊急停止 |
| 7 | BACK | 緊急停止 |
| 8 | （未使用） | |
| 9 | LSB | RSB との同時押しで緊急停止解除 |
| 10 | RSB | LSB との同時押しで緊急停止解除 |

## 6. 緊急停止

- **REQ-20** 受信した joy で LB / RB / START / BACK のいずれかが押されていたら、**対象車両の全部**へ、その4ボタンすべてを 1 にして送る。選択の状態を問わない。未選択でも送る。
- **REQ-21** 緊急停止を送るのは、押されている joy を受信している間だけ。manager は緊急停止を保持しない。
  保持は車両側が行う（§7）。`joy_node` は `autorepeat_rate=20.0` で押下中も送り続けるため、押している限り繰り返し届く。
- **REQ-22** 緊急停止ボタンが押されていても、axes のマスクは REQ-16 / REQ-17 のまま変えない。
- **REQ-23** 解除（LSB と RSB の同時押し）は他のボタンと同じ扱いとする。**選択車にだけ届き、非選択車には 0 が送られる**。
  全台を戻すときは全台選択にして解除する。1台ずつ戻すときは選択を切り替えながら解除する。

## 7. 車両側の前提

manager の設計は `racing_kart_driver` の次の挙動に依存する。

- **緊急停止はラッチする。** 4ボタンのいずれかが 1 の joy を1つ受け取った時点で保持され、LSB+RSB を受け取るまで解除されない。manager 側で保持や再送をしなくてよいのはこのため。
- **joy が5秒（`joy_delay_threshold`）届かないと緊急停止がラッチする。**
- **axes / buttons の要素数が 8 / 11 と一致しない joy は使われない。** 停止指令が出るだけで、緊急停止のラッチもされない。
- **control_mode が AUTONOMOUS のとき、アクセル・ブレーキ・ステアは Autoware の指令で上書きされる。** joy の軸は効かない。無操作値を送っても自動走行は止まらない。

## 8. ROS インタフェース

| 向き | topic | 型 | QoS |
| --- | --- | --- | --- |
| 購読 | `/racing_kart/joy` | `sensor_msgs/Joy` | depth 1 |
| 配信 | `/<VEHICLE_ID>/racing_kart/joy` | `sensor_msgs/Joy` | depth 1 |

`/<VEHICLE_ID>/racing_kart/joy` は zenoh ブリッジが車両側の `/racing_kart/joy` へ届ける。
車両IDの名前空間を付けるのは manager の役目である。

- **REQ-24** 依存するメッセージパッケージは `sensor_msgs` だけとする。

## 9. 保証しないこと

次は manager の責任外とし、GUI にも表示しない。

- **車両が停止しているか、緊急停止中か、どの control_mode か。** テレメトリを購読しないため知り得ない。状態は RViz と車両側のログで確認する。
- **非選択車が走っていないこと。** 非選択車には無操作の joy が届くが、その車が AUTONOMOUS なら Autoware の指令で走り続ける（§7）。
- **全台選択中に1台だけを操作すること。** 全台選択中は軸もボタンも全車へ等しく届く。Y を押せば全車が同時に自動運転へ入り、アクセルを踏めば全車が同時に走る。
- **選択の切り替え時に急発進しないこと。** アクセルを踏んだまま切り替えれば、切り替え先はその踏み込み量で動き出す。
- **緊急停止が全車へ届いたことの確認。** 届いたかどうかは manager には分からない。

## 10. テスト観点

ROS を起動せず pytest だけで走る。`racing_kart_manager_core` の純関数を検証する。

### 選択

| ID | 観点 | 対応 |
| --- | --- | --- |
| T-01 | 起動直後は未選択 | REQ-10 |
| T-02 | 単車を選ぶとその1台だけが選択車になる | REQ-09 |
| T-03 | 全台を選ぶと対象車両の全部が選択車になる | REQ-08, REQ-09 |
| T-04 | 未選択に戻せる | REQ-09 |
| T-05 | アクセルを踏んだ状態でも選択は切り替わる | REQ-12 |
| T-06 | 空・未知ID・重複の起動引数を拒否する | REQ-07 |

### 送出

| ID | 観点 | 対応 |
| --- | --- | --- |
| T-07 | joy 1つにつき対象車両全部へ1つずつ送る | REQ-13 |
| T-08 | 未選択でも全車へ送り、軸は無操作値になる | REQ-14, REQ-17 |
| T-09 | 選択車には軸もボタンも素通しする | REQ-16 |
| T-10 | 非選択車の軸が無操作値になる | REQ-17 |
| T-11 | 非選択車の A / B / X / Y と LSB / RSB が 0 になる | REQ-17 |
| T-12 | 送出する joy は常に axes 8 / buttons 11 | REQ-15 |
| T-13 | `header.stamp` が入力から引き継がれる | REQ-19 |

### 緊急停止

| ID | 観点 | 対応 |
| --- | --- | --- |
| T-14 | 単車選択中に緊急停止を押すと、全車の4ボタンが 1 になる | REQ-20 |
| T-15 | 未選択でも全車の4ボタンが 1 になる | REQ-20 |
| T-16 | 全台選択中も同じ | REQ-20 |
| T-17 | 4ボタンそれぞれ単独で成立する | REQ-20 |
| T-18 | 押されていない joy を受けたら 1 は立たない（保持しない） | REQ-21 |
| T-19 | 緊急停止中も選択車の軸は素通し、非選択車の軸は無操作値 | REQ-22 |
| T-20 | 要素数が規定と異なる joy では全車が非選択車扱いになり、読み取れた緊急停止ボタンは反映される | REQ-18 |

### 解除

| ID | 観点 | 対応 |
| --- | --- | --- |
| T-21 | LSB+RSB は選択車にだけ届く | REQ-23 |
| T-22 | 全台選択中は全車に届く | REQ-23 |

### 一斉指令

| ID | 観点 | 対応 |
| --- | --- | --- |
| T-41 | レース開始は選択を問わず全宛先の Y を立てる | REQ-30, REQ-31 |
| T-42 | レース終了は全宛先の X を立て、アクセルを無操作値にする（ブレーキには触れない） | REQ-31 |
| T-43 | 1回の押下で決まった数の joy に乗り、そのあと止まる | REQ-29, REQ-33 |
| T-44 | 繰り返しの途中で押したら、あとの指令で置き換える | REQ-34 |
| T-45 | 緊急停止と同時でも4ボタンは立ったまま | REQ-35 |
| T-46 | 要素数が規定と異なる joy のフレームでも届く | REQ-36 |

REQ-28（レース開始ボタンが選択を全台にする）は GUI 側の1行で、Tk を起こさないと確かめられないため
テストを置いていない。

## 11. 実験用: ブレーキ試験

車両のブレーキ入力に対する減速度を測るための機能。**既定では存在しない。**
起動引数 `--brake-test <PERCENT>` を渡したときだけ有効になる。

自動走行で直線に入った瞬間に B を押すと、ステアを自動に保ったまま一定のブレーキが入る。
手でトリガーを引くと踏み込み量が試行ごとにばらつき、何 % 入れたのかも分からないため、
値を固定して押すだけにする。

- **REQ-25** B（index 1）を押している間、選択車へ送る joy を次のように差し替える。
  それ以外の軸とボタンには触れない。

  | 差し替えるもの | 値 | 車両側の効果 |
  | --- | --- | --- |
  | ボタン X | `1` | `control_mode` を `AUTONOMOUS_STEER_ONLY` へ。ステアは Autoware、アクセルとブレーキが joy へ移る |
  | ブレーキ軸 | `1.0 - 2r` | 指定した比率 `r` のブレーキ |
  | アクセル軸 | `+1.0` | スロットルカット |

  アクセルを落とすのは、`AUTONOMOUS_STEER_ONLY` ではアクセルも joy 側になるため。
  落とさないと、トリガーを踏んでいたときにブレーキと同時に入る。

- **REQ-26** 効かせるのは**単車選択のとき、B を押している間、要素数が規定どおりの joy**
  に限る。全台選択中に全車が同時に急制動するのは事故のもとであり、壊れた入力では
  どの車両も操縦できない（REQ-18）。
- **REQ-27** `--brake-test` を渡さなければ B は他のボタンと同じく素通しする。

### 知っておく必要がある挙動

**B を離しても自動運転には戻らない。** 車両側の `control_mode` はラッチで、X を1フレーム
でも受け取ればそこで固定される。離したあとは自動操舵のまま惰行する。走行に戻すには Y を、
止めるには緊急停止を押す。オペレータの A / X / Y は素通しのままなので、そのまま押せる。

テレメトリを購読しないため、`control_mode` が実際に切り替わったかを遠隔側から確認する
手段は無い（§10）。車の挙動で判断する。

スロットルカットを伴うので、測っているのは「一定速度からブレーキ」ではなく
「スロットルカット + ブレーキ r」の減速度である。ブレーキの寄与を切り分けるには、
B を押さずに X だけを押した回をベースラインとして取る。

### テスト観点

| ID | 観点 | 対応 |
| --- | --- | --- |
| T-35 | 指定した比率が車両側の式でそのまま読める | REQ-25 |
| T-36 | X を立て、ブレーキを指定値に、アクセルを無操作値にする | REQ-25 |
| T-37 | 押している間だけ効く | REQ-26 |
| T-38 | 単車選択のときだけ効き、非選択車には無操作値が行く | REQ-17, REQ-26 |
| T-39 | 起動引数を渡さなければ B は素通しする | REQ-27 |
| T-40 | 要素数が規定と異なる joy では効かない | REQ-26 |
